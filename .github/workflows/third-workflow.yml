name: Third Workflow
on: push

jobs: 
    run-first-command:
        runs-on: ubuntu-latest
        steps:
            - name: Display some text
              run: echo "Some text from third workflow"
            - name: Display Python Version on ubuntu
              run: |
                pwd
                ls -a